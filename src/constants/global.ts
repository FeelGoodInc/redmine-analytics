export const PROJECT_NAME = 'Redmine Anal';

export const TOKEN_COOKIE_NAME = 'rm_api_key';

export const SIDE_MENU_STATE_COOKIE_NAME = 'rma.sidemenu.state';

export const API_URL = process.env.NEXT_PUBLIC_API_URL;

export const AUTH_PAGE_URL = '/auth';

export const HOME_PAGE_URL = '/dashboard';

export const BACKEND_PROXY_URL_PREFIX = '/backend';

export const RESET_CREDENTIALS_URL = '/reset-credentials';

export const APP_VERSION = process.env.NEXT_PUBLIC_VERSION;